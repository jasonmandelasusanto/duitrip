{
  "name": "duitrip-firebase",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-duitrip \"node test/rules.test.js\""
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0"
  }
}
