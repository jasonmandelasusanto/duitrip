<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme applied before Compose takes over. Dark, matching the app background. -->
    <style name="Theme.Duitrip" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/bg_base</item>
        <item name="android:navigationBarColor">@color/bg_base</item>
        <item name="android:windowBackground">@color/bg_base</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
